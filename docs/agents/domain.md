# Domain Docs

This is a single-context repository.

Before exploring or changing the project, read root `CONTEXT.md` and the ADRs
in `docs/adr/` that govern the affected area. Use the glossary's terms in
issues, contracts, tests, and UI text. Surface conflicts with an ADR explicitly
rather than silently overriding it.
